language: cpp
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake
script:
  -git submodule update --init --recursive
  -mkdir build
  -cd build
  -cmake ..
  -make
